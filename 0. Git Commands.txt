git init
git clone 
    (clone the remote repo url)

git add .
git commit -m "message"
git push -u origin main 
    (as we are on main branch)

git pull origin main 
    (to pull the data from remorte repo to local system)

git status
git branch 
git merge
git log
